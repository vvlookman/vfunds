title = "持有满足一定条件且溢价最高的可转债"

[[rules]]
name = "hold_top_conv_bond"
frequency = "4w"

[rules.options]
filter_issue_size_floor = 1
filter_min_remaining_days = 30
limit = 5

[rules.search]
filter_issue_size_floor = [0.25, 0.5, 1, 2]
limit = [3, 5, 10]
filter_min_remaining_days = [0, 30, 60, 90, 120]
